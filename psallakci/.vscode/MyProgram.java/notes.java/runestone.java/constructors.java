public class constructors {
    public static void main(String[] args) {
        // a class defines an object's attributes and methods
        // each class has constructors
        // constructors: initializes the attributes in a newly created object
        // constructor has the same name as the class.
        // "new" keybord is used to create a new object followed by the class name = new
        // Class()
        // to create a new object and call a constructor:
        // ClassName variableName = new ClassName (parameters);

        // overloaded consructors: when there are more than one constructor in a class.
        // no-argument constructor (constructor that has no parameters): World()

        // parameter(argument): value that is passed into a constructor.
        // (used to initialize the attribute of an object)

        // World () automatically creates a graphical window with 640x480 pixels

        // the order of the parameters matter

        // An object variable can be declared and initialized to null
        // (Turtle t1 = null)
        // reference: way to find the object in memory.
        // object variable holds a reference to an object.

        // when a class is already written the methods and information about...
        // ...how to use constructors is written in the documentation
        // the documentation lists the signatures (headers) of consturctors
        // or methods which will tell you their name and parameter list.
        // parameter list: it's in the header of a constructor, it lists the formal
        // parameters
        // formal parameters: declares the variables as values and their data types.
        // overloaded constructors has different signatures.
        // differ in: number, type, order of parameters
        // constructor headers(signatures): first line of the constructors, showing
        // their name and paremeters.

        // instance variables: int, double, boolean...
        // formal parameters: for ex: (year, month, day)
        // actual parameters: for ex: (2005, 9, 20)
        // call by value: formal parameters are set to copies of the actual parameters.
        // call by value's are used to initialize the object's attributes.
    }
}
