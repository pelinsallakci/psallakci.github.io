public class exercises {
    public static void main(String[] args) {
        // 1st question:
        // int t = a sets t to be equal to a, (t = a).
        // b = t sets b to be equal to t, so now (b = a).
        // a = b sets a to be equal to b, so (a = original value of a).
        // so basically t and b is being equal to the original value of a.

        // 4th question:
        // !(a < b) && !(a > b))
        // if we simplify the statement: (a >= b) && (a <= b)
        // so a == b

        // 5th question:
        // false ^ false = false
        // true ^ true = false
        // false ^ true = true
        // true ^ false = true

        // 6th question:
        // 10/3 gives 3 and not 3.33333333
        // Since, 10 and 3 are integers. If two of them were double the answer would be
        // 3.33333333

        // 7th question:
        // a. 2bc : since concatenation occurs
        // b. 5bc : when numbers come first sum is computed.
        // c. 5bc : if there are parentheseses sum should be computed.
        // d. bc5 : if there are parentheseses sum should be computed.
        // e. bc23 : since there are no parentheseses.

        // 8th question:
        // in order to find the square root of a number c
        // we should find the roots of (x*x) + 0*x - c.

        // 16th question:
        // the problem is G * mass1 * mass2 will be divided by r insted of (r * r)
        // correct code: double force = G * mass1 * mass2 / (r * r);
    }
}
